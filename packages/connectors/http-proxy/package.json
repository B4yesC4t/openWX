{
  "name": "@openwx/connector-http-proxy",
  "version": "0.1.0",
  "description": "HTTP proxy connector scaffold for routing openWX requests to upstream services.",
  "keywords": [
    "openwx",
    "wechat",
    "connector",
    "http",
    "proxy",
    "ilink"
  ],
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/B4yesC4t/openWX.git",
    "directory": "packages/connectors/http-proxy"
  },
  "bugs": {
    "url": "https://github.com/B4yesC4t/openWX/issues"
  },
  "homepage": "https://github.com/B4yesC4t/openWX#readme",
  "dependencies": {
    "@openwx/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --out-dir dist --tsconfig tsconfig.build.json",
    "dev": "tsup src/index.ts --format esm --dts --watch --out-dir dist --tsconfig tsconfig.build.json",
    "lint": "eslint src tests",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
