{
  "name": "@openwx/example-assistant",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "17.2.3"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "tsx": "4.20.6",
    "typescript": "5.9.3",
    "vitest": "4.1.0"
  }
}
